// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/heshaofeng1991/entgo/ent/gen/internal"
	"github.com/heshaofeng1991/entgo/ent/gen/order"
	"github.com/heshaofeng1991/entgo/ent/gen/predicate"
	"github.com/heshaofeng1991/entgo/ent/gen/trackmapping"
)

// TrackMappingQuery is the builder for querying TrackMapping entities.
type TrackMappingQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.TrackMapping
	withOrders *OrderQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TrackMappingQuery builder.
func (tmq *TrackMappingQuery) Where(ps ...predicate.TrackMapping) *TrackMappingQuery {
	tmq.predicates = append(tmq.predicates, ps...)
	return tmq
}

// Limit adds a limit step to the query.
func (tmq *TrackMappingQuery) Limit(limit int) *TrackMappingQuery {
	tmq.limit = &limit
	return tmq
}

// Offset adds an offset step to the query.
func (tmq *TrackMappingQuery) Offset(offset int) *TrackMappingQuery {
	tmq.offset = &offset
	return tmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tmq *TrackMappingQuery) Unique(unique bool) *TrackMappingQuery {
	tmq.unique = &unique
	return tmq
}

// Order adds an order step to the query.
func (tmq *TrackMappingQuery) Order(o ...OrderFunc) *TrackMappingQuery {
	tmq.order = append(tmq.order, o...)
	return tmq
}

// QueryOrders chains the current query on the "orders" edge.
func (tmq *TrackMappingQuery) QueryOrders() *OrderQuery {
	query := &OrderQuery{config: tmq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(trackmapping.Table, trackmapping.FieldID, selector),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, trackmapping.OrdersTable, trackmapping.OrdersColumn),
		)
		schemaConfig := tmq.schemaConfig
		step.To.Schema = schemaConfig.Order
		step.Edge.Schema = schemaConfig.TrackMapping
		fromU = sqlgraph.SetNeighbors(tmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TrackMapping entity from the query.
// Returns a *NotFoundError when no TrackMapping was found.
func (tmq *TrackMappingQuery) First(ctx context.Context) (*TrackMapping, error) {
	nodes, err := tmq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{trackmapping.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tmq *TrackMappingQuery) FirstX(ctx context.Context) *TrackMapping {
	node, err := tmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TrackMapping ID from the query.
// Returns a *NotFoundError when no TrackMapping ID was found.
func (tmq *TrackMappingQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = tmq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{trackmapping.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tmq *TrackMappingQuery) FirstIDX(ctx context.Context) int64 {
	id, err := tmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TrackMapping entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TrackMapping entity is found.
// Returns a *NotFoundError when no TrackMapping entities are found.
func (tmq *TrackMappingQuery) Only(ctx context.Context) (*TrackMapping, error) {
	nodes, err := tmq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{trackmapping.Label}
	default:
		return nil, &NotSingularError{trackmapping.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tmq *TrackMappingQuery) OnlyX(ctx context.Context) *TrackMapping {
	node, err := tmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TrackMapping ID in the query.
// Returns a *NotSingularError when more than one TrackMapping ID is found.
// Returns a *NotFoundError when no entities are found.
func (tmq *TrackMappingQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = tmq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{trackmapping.Label}
	default:
		err = &NotSingularError{trackmapping.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tmq *TrackMappingQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := tmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TrackMappings.
func (tmq *TrackMappingQuery) All(ctx context.Context) ([]*TrackMapping, error) {
	if err := tmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tmq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tmq *TrackMappingQuery) AllX(ctx context.Context) []*TrackMapping {
	nodes, err := tmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TrackMapping IDs.
func (tmq *TrackMappingQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := tmq.Select(trackmapping.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tmq *TrackMappingQuery) IDsX(ctx context.Context) []int64 {
	ids, err := tmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tmq *TrackMappingQuery) Count(ctx context.Context) (int, error) {
	if err := tmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tmq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tmq *TrackMappingQuery) CountX(ctx context.Context) int {
	count, err := tmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tmq *TrackMappingQuery) Exist(ctx context.Context) (bool, error) {
	if err := tmq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tmq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tmq *TrackMappingQuery) ExistX(ctx context.Context) bool {
	exist, err := tmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TrackMappingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tmq *TrackMappingQuery) Clone() *TrackMappingQuery {
	if tmq == nil {
		return nil
	}
	return &TrackMappingQuery{
		config:     tmq.config,
		limit:      tmq.limit,
		offset:     tmq.offset,
		order:      append([]OrderFunc{}, tmq.order...),
		predicates: append([]predicate.TrackMapping{}, tmq.predicates...),
		withOrders: tmq.withOrders.Clone(),
		// clone intermediate query.
		sql:    tmq.sql.Clone(),
		path:   tmq.path,
		unique: tmq.unique,
	}
}

// WithOrders tells the query-builder to eager-load the nodes that are connected to
// the "orders" edge. The optional arguments are used to configure the query builder of the edge.
func (tmq *TrackMappingQuery) WithOrders(opts ...func(*OrderQuery)) *TrackMappingQuery {
	query := &OrderQuery{config: tmq.config}
	for _, opt := range opts {
		opt(query)
	}
	tmq.withOrders = query
	return tmq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TrackMapping.Query().
//		GroupBy(trackmapping.FieldCreatedAt).
//		Aggregate(gen.Count()).
//		Scan(ctx, &v)
func (tmq *TrackMappingQuery) GroupBy(field string, fields ...string) *TrackMappingGroupBy {
	grbuild := &TrackMappingGroupBy{config: tmq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tmq.sqlQuery(ctx), nil
	}
	grbuild.label = trackmapping.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.TrackMapping.Query().
//		Select(trackmapping.FieldCreatedAt).
//		Scan(ctx, &v)
func (tmq *TrackMappingQuery) Select(fields ...string) *TrackMappingSelect {
	tmq.fields = append(tmq.fields, fields...)
	selbuild := &TrackMappingSelect{TrackMappingQuery: tmq}
	selbuild.label = trackmapping.Label
	selbuild.flds, selbuild.scan = &tmq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a TrackMappingSelect configured with the given aggregations.
func (tmq *TrackMappingQuery) Aggregate(fns ...AggregateFunc) *TrackMappingSelect {
	return tmq.Select().Aggregate(fns...)
}

func (tmq *TrackMappingQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tmq.fields {
		if !trackmapping.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("gen: invalid field %q for query", f)}
		}
	}
	if tmq.path != nil {
		prev, err := tmq.path(ctx)
		if err != nil {
			return err
		}
		tmq.sql = prev
	}
	return nil
}

func (tmq *TrackMappingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TrackMapping, error) {
	var (
		nodes       = []*TrackMapping{}
		_spec       = tmq.querySpec()
		loadedTypes = [1]bool{
			tmq.withOrders != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TrackMapping).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TrackMapping{config: tmq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = tmq.schemaConfig.TrackMapping
	ctx = internal.NewSchemaConfigContext(ctx, tmq.schemaConfig)
	if len(tmq.modifiers) > 0 {
		_spec.Modifiers = tmq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tmq.withOrders; query != nil {
		if err := tmq.loadOrders(ctx, query, nodes, nil,
			func(n *TrackMapping, e *Order) { n.Edges.Orders = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tmq *TrackMappingQuery) loadOrders(ctx context.Context, query *OrderQuery, nodes []*TrackMapping, init func(*TrackMapping), assign func(*TrackMapping, *Order)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*TrackMapping)
	for i := range nodes {
		fk := nodes[i].OrderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(order.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "order_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tmq *TrackMappingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tmq.querySpec()
	_spec.Node.Schema = tmq.schemaConfig.TrackMapping
	ctx = internal.NewSchemaConfigContext(ctx, tmq.schemaConfig)
	if len(tmq.modifiers) > 0 {
		_spec.Modifiers = tmq.modifiers
	}
	_spec.Node.Columns = tmq.fields
	if len(tmq.fields) > 0 {
		_spec.Unique = tmq.unique != nil && *tmq.unique
	}
	return sqlgraph.CountNodes(ctx, tmq.driver, _spec)
}

func (tmq *TrackMappingQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := tmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("gen: check existence: %w", err)
	default:
		return true, nil
	}
}

func (tmq *TrackMappingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   trackmapping.Table,
			Columns: trackmapping.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: trackmapping.FieldID,
			},
		},
		From:   tmq.sql,
		Unique: true,
	}
	if unique := tmq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tmq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, trackmapping.FieldID)
		for i := range fields {
			if fields[i] != trackmapping.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tmq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tmq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tmq *TrackMappingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tmq.driver.Dialect())
	t1 := builder.Table(trackmapping.Table)
	columns := tmq.fields
	if len(columns) == 0 {
		columns = trackmapping.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tmq.sql != nil {
		selector = tmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tmq.unique != nil && *tmq.unique {
		selector.Distinct()
	}
	t1.Schema(tmq.schemaConfig.TrackMapping)
	ctx = internal.NewSchemaConfigContext(ctx, tmq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range tmq.modifiers {
		m(selector)
	}
	for _, p := range tmq.predicates {
		p(selector)
	}
	for _, p := range tmq.order {
		p(selector)
	}
	if offset := tmq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tmq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tmq *TrackMappingQuery) Modify(modifiers ...func(s *sql.Selector)) *TrackMappingSelect {
	tmq.modifiers = append(tmq.modifiers, modifiers...)
	return tmq.Select()
}

// TrackMappingGroupBy is the group-by builder for TrackMapping entities.
type TrackMappingGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tmgb *TrackMappingGroupBy) Aggregate(fns ...AggregateFunc) *TrackMappingGroupBy {
	tmgb.fns = append(tmgb.fns, fns...)
	return tmgb
}

// Scan applies the group-by query and scans the result into the given value.
func (tmgb *TrackMappingGroupBy) Scan(ctx context.Context, v any) error {
	query, err := tmgb.path(ctx)
	if err != nil {
		return err
	}
	tmgb.sql = query
	return tmgb.sqlScan(ctx, v)
}

func (tmgb *TrackMappingGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range tmgb.fields {
		if !trackmapping.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tmgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tmgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tmgb *TrackMappingGroupBy) sqlQuery() *sql.Selector {
	selector := tmgb.sql.Select()
	aggregation := make([]string, 0, len(tmgb.fns))
	for _, fn := range tmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tmgb.fields)+len(tmgb.fns))
		for _, f := range tmgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tmgb.fields...)...)
}

// TrackMappingSelect is the builder for selecting fields of TrackMapping entities.
type TrackMappingSelect struct {
	*TrackMappingQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tms *TrackMappingSelect) Aggregate(fns ...AggregateFunc) *TrackMappingSelect {
	tms.fns = append(tms.fns, fns...)
	return tms
}

// Scan applies the selector query and scans the result into the given value.
func (tms *TrackMappingSelect) Scan(ctx context.Context, v any) error {
	if err := tms.prepareQuery(ctx); err != nil {
		return err
	}
	tms.sql = tms.TrackMappingQuery.sqlQuery(ctx)
	return tms.sqlScan(ctx, v)
}

func (tms *TrackMappingSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(tms.fns))
	for _, fn := range tms.fns {
		aggregation = append(aggregation, fn(tms.sql))
	}
	switch n := len(*tms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		tms.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		tms.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := tms.sql.Query()
	if err := tms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tms *TrackMappingSelect) Modify(modifiers ...func(s *sql.Selector)) *TrackMappingSelect {
	tms.modifiers = append(tms.modifiers, modifiers...)
	return tms
}
