// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/heshaofeng1991/entgo/ent/gen/internal"
	"github.com/heshaofeng1991/entgo/ent/gen/predicate"
	"github.com/heshaofeng1991/entgo/ent/gen/valueaddedtax"
)

// ValueAddedTaxQuery is the builder for querying ValueAddedTax entities.
type ValueAddedTaxQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ValueAddedTax
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ValueAddedTaxQuery builder.
func (vatq *ValueAddedTaxQuery) Where(ps ...predicate.ValueAddedTax) *ValueAddedTaxQuery {
	vatq.predicates = append(vatq.predicates, ps...)
	return vatq
}

// Limit adds a limit step to the query.
func (vatq *ValueAddedTaxQuery) Limit(limit int) *ValueAddedTaxQuery {
	vatq.limit = &limit
	return vatq
}

// Offset adds an offset step to the query.
func (vatq *ValueAddedTaxQuery) Offset(offset int) *ValueAddedTaxQuery {
	vatq.offset = &offset
	return vatq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vatq *ValueAddedTaxQuery) Unique(unique bool) *ValueAddedTaxQuery {
	vatq.unique = &unique
	return vatq
}

// Order adds an order step to the query.
func (vatq *ValueAddedTaxQuery) Order(o ...OrderFunc) *ValueAddedTaxQuery {
	vatq.order = append(vatq.order, o...)
	return vatq
}

// First returns the first ValueAddedTax entity from the query.
// Returns a *NotFoundError when no ValueAddedTax was found.
func (vatq *ValueAddedTaxQuery) First(ctx context.Context) (*ValueAddedTax, error) {
	nodes, err := vatq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{valueaddedtax.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vatq *ValueAddedTaxQuery) FirstX(ctx context.Context) *ValueAddedTax {
	node, err := vatq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ValueAddedTax ID from the query.
// Returns a *NotFoundError when no ValueAddedTax ID was found.
func (vatq *ValueAddedTaxQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = vatq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{valueaddedtax.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vatq *ValueAddedTaxQuery) FirstIDX(ctx context.Context) int64 {
	id, err := vatq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ValueAddedTax entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ValueAddedTax entity is found.
// Returns a *NotFoundError when no ValueAddedTax entities are found.
func (vatq *ValueAddedTaxQuery) Only(ctx context.Context) (*ValueAddedTax, error) {
	nodes, err := vatq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{valueaddedtax.Label}
	default:
		return nil, &NotSingularError{valueaddedtax.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vatq *ValueAddedTaxQuery) OnlyX(ctx context.Context) *ValueAddedTax {
	node, err := vatq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ValueAddedTax ID in the query.
// Returns a *NotSingularError when more than one ValueAddedTax ID is found.
// Returns a *NotFoundError when no entities are found.
func (vatq *ValueAddedTaxQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = vatq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{valueaddedtax.Label}
	default:
		err = &NotSingularError{valueaddedtax.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vatq *ValueAddedTaxQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := vatq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ValueAddedTaxes.
func (vatq *ValueAddedTaxQuery) All(ctx context.Context) ([]*ValueAddedTax, error) {
	if err := vatq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return vatq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (vatq *ValueAddedTaxQuery) AllX(ctx context.Context) []*ValueAddedTax {
	nodes, err := vatq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ValueAddedTax IDs.
func (vatq *ValueAddedTaxQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := vatq.Select(valueaddedtax.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vatq *ValueAddedTaxQuery) IDsX(ctx context.Context) []int64 {
	ids, err := vatq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vatq *ValueAddedTaxQuery) Count(ctx context.Context) (int, error) {
	if err := vatq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return vatq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (vatq *ValueAddedTaxQuery) CountX(ctx context.Context) int {
	count, err := vatq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vatq *ValueAddedTaxQuery) Exist(ctx context.Context) (bool, error) {
	if err := vatq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return vatq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (vatq *ValueAddedTaxQuery) ExistX(ctx context.Context) bool {
	exist, err := vatq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ValueAddedTaxQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vatq *ValueAddedTaxQuery) Clone() *ValueAddedTaxQuery {
	if vatq == nil {
		return nil
	}
	return &ValueAddedTaxQuery{
		config:     vatq.config,
		limit:      vatq.limit,
		offset:     vatq.offset,
		order:      append([]OrderFunc{}, vatq.order...),
		predicates: append([]predicate.ValueAddedTax{}, vatq.predicates...),
		// clone intermediate query.
		sql:    vatq.sql.Clone(),
		path:   vatq.path,
		unique: vatq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ValueAddedTax.Query().
//		GroupBy(valueaddedtax.FieldCreatedAt).
//		Aggregate(gen.Count()).
//		Scan(ctx, &v)
func (vatq *ValueAddedTaxQuery) GroupBy(field string, fields ...string) *ValueAddedTaxGroupBy {
	grbuild := &ValueAddedTaxGroupBy{config: vatq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := vatq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return vatq.sqlQuery(ctx), nil
	}
	grbuild.label = valueaddedtax.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ValueAddedTax.Query().
//		Select(valueaddedtax.FieldCreatedAt).
//		Scan(ctx, &v)
func (vatq *ValueAddedTaxQuery) Select(fields ...string) *ValueAddedTaxSelect {
	vatq.fields = append(vatq.fields, fields...)
	selbuild := &ValueAddedTaxSelect{ValueAddedTaxQuery: vatq}
	selbuild.label = valueaddedtax.Label
	selbuild.flds, selbuild.scan = &vatq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a ValueAddedTaxSelect configured with the given aggregations.
func (vatq *ValueAddedTaxQuery) Aggregate(fns ...AggregateFunc) *ValueAddedTaxSelect {
	return vatq.Select().Aggregate(fns...)
}

func (vatq *ValueAddedTaxQuery) prepareQuery(ctx context.Context) error {
	for _, f := range vatq.fields {
		if !valueaddedtax.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("gen: invalid field %q for query", f)}
		}
	}
	if vatq.path != nil {
		prev, err := vatq.path(ctx)
		if err != nil {
			return err
		}
		vatq.sql = prev
	}
	return nil
}

func (vatq *ValueAddedTaxQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ValueAddedTax, error) {
	var (
		nodes = []*ValueAddedTax{}
		_spec = vatq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ValueAddedTax).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ValueAddedTax{config: vatq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = vatq.schemaConfig.ValueAddedTax
	ctx = internal.NewSchemaConfigContext(ctx, vatq.schemaConfig)
	if len(vatq.modifiers) > 0 {
		_spec.Modifiers = vatq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vatq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (vatq *ValueAddedTaxQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vatq.querySpec()
	_spec.Node.Schema = vatq.schemaConfig.ValueAddedTax
	ctx = internal.NewSchemaConfigContext(ctx, vatq.schemaConfig)
	if len(vatq.modifiers) > 0 {
		_spec.Modifiers = vatq.modifiers
	}
	_spec.Node.Columns = vatq.fields
	if len(vatq.fields) > 0 {
		_spec.Unique = vatq.unique != nil && *vatq.unique
	}
	return sqlgraph.CountNodes(ctx, vatq.driver, _spec)
}

func (vatq *ValueAddedTaxQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := vatq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("gen: check existence: %w", err)
	default:
		return true, nil
	}
}

func (vatq *ValueAddedTaxQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   valueaddedtax.Table,
			Columns: valueaddedtax.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: valueaddedtax.FieldID,
			},
		},
		From:   vatq.sql,
		Unique: true,
	}
	if unique := vatq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := vatq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, valueaddedtax.FieldID)
		for i := range fields {
			if fields[i] != valueaddedtax.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vatq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vatq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vatq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vatq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vatq *ValueAddedTaxQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vatq.driver.Dialect())
	t1 := builder.Table(valueaddedtax.Table)
	columns := vatq.fields
	if len(columns) == 0 {
		columns = valueaddedtax.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vatq.sql != nil {
		selector = vatq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vatq.unique != nil && *vatq.unique {
		selector.Distinct()
	}
	t1.Schema(vatq.schemaConfig.ValueAddedTax)
	ctx = internal.NewSchemaConfigContext(ctx, vatq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range vatq.modifiers {
		m(selector)
	}
	for _, p := range vatq.predicates {
		p(selector)
	}
	for _, p := range vatq.order {
		p(selector)
	}
	if offset := vatq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vatq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (vatq *ValueAddedTaxQuery) Modify(modifiers ...func(s *sql.Selector)) *ValueAddedTaxSelect {
	vatq.modifiers = append(vatq.modifiers, modifiers...)
	return vatq.Select()
}

// ValueAddedTaxGroupBy is the group-by builder for ValueAddedTax entities.
type ValueAddedTaxGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vatgb *ValueAddedTaxGroupBy) Aggregate(fns ...AggregateFunc) *ValueAddedTaxGroupBy {
	vatgb.fns = append(vatgb.fns, fns...)
	return vatgb
}

// Scan applies the group-by query and scans the result into the given value.
func (vatgb *ValueAddedTaxGroupBy) Scan(ctx context.Context, v any) error {
	query, err := vatgb.path(ctx)
	if err != nil {
		return err
	}
	vatgb.sql = query
	return vatgb.sqlScan(ctx, v)
}

func (vatgb *ValueAddedTaxGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range vatgb.fields {
		if !valueaddedtax.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := vatgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vatgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (vatgb *ValueAddedTaxGroupBy) sqlQuery() *sql.Selector {
	selector := vatgb.sql.Select()
	aggregation := make([]string, 0, len(vatgb.fns))
	for _, fn := range vatgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(vatgb.fields)+len(vatgb.fns))
		for _, f := range vatgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(vatgb.fields...)...)
}

// ValueAddedTaxSelect is the builder for selecting fields of ValueAddedTax entities.
type ValueAddedTaxSelect struct {
	*ValueAddedTaxQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vats *ValueAddedTaxSelect) Aggregate(fns ...AggregateFunc) *ValueAddedTaxSelect {
	vats.fns = append(vats.fns, fns...)
	return vats
}

// Scan applies the selector query and scans the result into the given value.
func (vats *ValueAddedTaxSelect) Scan(ctx context.Context, v any) error {
	if err := vats.prepareQuery(ctx); err != nil {
		return err
	}
	vats.sql = vats.ValueAddedTaxQuery.sqlQuery(ctx)
	return vats.sqlScan(ctx, v)
}

func (vats *ValueAddedTaxSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(vats.fns))
	for _, fn := range vats.fns {
		aggregation = append(aggregation, fn(vats.sql))
	}
	switch n := len(*vats.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		vats.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		vats.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := vats.sql.Query()
	if err := vats.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (vats *ValueAddedTaxSelect) Modify(modifiers ...func(s *sql.Selector)) *ValueAddedTaxSelect {
	vats.modifiers = append(vats.modifiers, modifiers...)
	return vats
}
