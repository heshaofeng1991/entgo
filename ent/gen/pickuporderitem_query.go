// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/heshaofeng1991/entgo/ent/gen/internal"
	"github.com/heshaofeng1991/entgo/ent/gen/pickuporderitem"
	"github.com/heshaofeng1991/entgo/ent/gen/predicate"
)

// PickupOrderItemQuery is the builder for querying PickupOrderItem entities.
type PickupOrderItemQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.PickupOrderItem
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PickupOrderItemQuery builder.
func (poiq *PickupOrderItemQuery) Where(ps ...predicate.PickupOrderItem) *PickupOrderItemQuery {
	poiq.predicates = append(poiq.predicates, ps...)
	return poiq
}

// Limit adds a limit step to the query.
func (poiq *PickupOrderItemQuery) Limit(limit int) *PickupOrderItemQuery {
	poiq.limit = &limit
	return poiq
}

// Offset adds an offset step to the query.
func (poiq *PickupOrderItemQuery) Offset(offset int) *PickupOrderItemQuery {
	poiq.offset = &offset
	return poiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (poiq *PickupOrderItemQuery) Unique(unique bool) *PickupOrderItemQuery {
	poiq.unique = &unique
	return poiq
}

// Order adds an order step to the query.
func (poiq *PickupOrderItemQuery) Order(o ...OrderFunc) *PickupOrderItemQuery {
	poiq.order = append(poiq.order, o...)
	return poiq
}

// First returns the first PickupOrderItem entity from the query.
// Returns a *NotFoundError when no PickupOrderItem was found.
func (poiq *PickupOrderItemQuery) First(ctx context.Context) (*PickupOrderItem, error) {
	nodes, err := poiq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pickuporderitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (poiq *PickupOrderItemQuery) FirstX(ctx context.Context) *PickupOrderItem {
	node, err := poiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PickupOrderItem ID from the query.
// Returns a *NotFoundError when no PickupOrderItem ID was found.
func (poiq *PickupOrderItemQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = poiq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pickuporderitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (poiq *PickupOrderItemQuery) FirstIDX(ctx context.Context) int64 {
	id, err := poiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PickupOrderItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PickupOrderItem entity is found.
// Returns a *NotFoundError when no PickupOrderItem entities are found.
func (poiq *PickupOrderItemQuery) Only(ctx context.Context) (*PickupOrderItem, error) {
	nodes, err := poiq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pickuporderitem.Label}
	default:
		return nil, &NotSingularError{pickuporderitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (poiq *PickupOrderItemQuery) OnlyX(ctx context.Context) *PickupOrderItem {
	node, err := poiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PickupOrderItem ID in the query.
// Returns a *NotSingularError when more than one PickupOrderItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (poiq *PickupOrderItemQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = poiq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pickuporderitem.Label}
	default:
		err = &NotSingularError{pickuporderitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (poiq *PickupOrderItemQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := poiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PickupOrderItems.
func (poiq *PickupOrderItemQuery) All(ctx context.Context) ([]*PickupOrderItem, error) {
	if err := poiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return poiq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (poiq *PickupOrderItemQuery) AllX(ctx context.Context) []*PickupOrderItem {
	nodes, err := poiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PickupOrderItem IDs.
func (poiq *PickupOrderItemQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := poiq.Select(pickuporderitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (poiq *PickupOrderItemQuery) IDsX(ctx context.Context) []int64 {
	ids, err := poiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (poiq *PickupOrderItemQuery) Count(ctx context.Context) (int, error) {
	if err := poiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return poiq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (poiq *PickupOrderItemQuery) CountX(ctx context.Context) int {
	count, err := poiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (poiq *PickupOrderItemQuery) Exist(ctx context.Context) (bool, error) {
	if err := poiq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return poiq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (poiq *PickupOrderItemQuery) ExistX(ctx context.Context) bool {
	exist, err := poiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PickupOrderItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (poiq *PickupOrderItemQuery) Clone() *PickupOrderItemQuery {
	if poiq == nil {
		return nil
	}
	return &PickupOrderItemQuery{
		config:     poiq.config,
		limit:      poiq.limit,
		offset:     poiq.offset,
		order:      append([]OrderFunc{}, poiq.order...),
		predicates: append([]predicate.PickupOrderItem{}, poiq.predicates...),
		// clone intermediate query.
		sql:    poiq.sql.Clone(),
		path:   poiq.path,
		unique: poiq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PickupOrderItem.Query().
//		GroupBy(pickuporderitem.FieldCreatedAt).
//		Aggregate(gen.Count()).
//		Scan(ctx, &v)
func (poiq *PickupOrderItemQuery) GroupBy(field string, fields ...string) *PickupOrderItemGroupBy {
	grbuild := &PickupOrderItemGroupBy{config: poiq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := poiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return poiq.sqlQuery(ctx), nil
	}
	grbuild.label = pickuporderitem.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PickupOrderItem.Query().
//		Select(pickuporderitem.FieldCreatedAt).
//		Scan(ctx, &v)
func (poiq *PickupOrderItemQuery) Select(fields ...string) *PickupOrderItemSelect {
	poiq.fields = append(poiq.fields, fields...)
	selbuild := &PickupOrderItemSelect{PickupOrderItemQuery: poiq}
	selbuild.label = pickuporderitem.Label
	selbuild.flds, selbuild.scan = &poiq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a PickupOrderItemSelect configured with the given aggregations.
func (poiq *PickupOrderItemQuery) Aggregate(fns ...AggregateFunc) *PickupOrderItemSelect {
	return poiq.Select().Aggregate(fns...)
}

func (poiq *PickupOrderItemQuery) prepareQuery(ctx context.Context) error {
	for _, f := range poiq.fields {
		if !pickuporderitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("gen: invalid field %q for query", f)}
		}
	}
	if poiq.path != nil {
		prev, err := poiq.path(ctx)
		if err != nil {
			return err
		}
		poiq.sql = prev
	}
	return nil
}

func (poiq *PickupOrderItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PickupOrderItem, error) {
	var (
		nodes = []*PickupOrderItem{}
		_spec = poiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PickupOrderItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PickupOrderItem{config: poiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = poiq.schemaConfig.PickupOrderItem
	ctx = internal.NewSchemaConfigContext(ctx, poiq.schemaConfig)
	if len(poiq.modifiers) > 0 {
		_spec.Modifiers = poiq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, poiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (poiq *PickupOrderItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := poiq.querySpec()
	_spec.Node.Schema = poiq.schemaConfig.PickupOrderItem
	ctx = internal.NewSchemaConfigContext(ctx, poiq.schemaConfig)
	if len(poiq.modifiers) > 0 {
		_spec.Modifiers = poiq.modifiers
	}
	_spec.Node.Columns = poiq.fields
	if len(poiq.fields) > 0 {
		_spec.Unique = poiq.unique != nil && *poiq.unique
	}
	return sqlgraph.CountNodes(ctx, poiq.driver, _spec)
}

func (poiq *PickupOrderItemQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := poiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("gen: check existence: %w", err)
	default:
		return true, nil
	}
}

func (poiq *PickupOrderItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   pickuporderitem.Table,
			Columns: pickuporderitem.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: pickuporderitem.FieldID,
			},
		},
		From:   poiq.sql,
		Unique: true,
	}
	if unique := poiq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := poiq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pickuporderitem.FieldID)
		for i := range fields {
			if fields[i] != pickuporderitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := poiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := poiq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := poiq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := poiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (poiq *PickupOrderItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(poiq.driver.Dialect())
	t1 := builder.Table(pickuporderitem.Table)
	columns := poiq.fields
	if len(columns) == 0 {
		columns = pickuporderitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if poiq.sql != nil {
		selector = poiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if poiq.unique != nil && *poiq.unique {
		selector.Distinct()
	}
	t1.Schema(poiq.schemaConfig.PickupOrderItem)
	ctx = internal.NewSchemaConfigContext(ctx, poiq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range poiq.modifiers {
		m(selector)
	}
	for _, p := range poiq.predicates {
		p(selector)
	}
	for _, p := range poiq.order {
		p(selector)
	}
	if offset := poiq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := poiq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (poiq *PickupOrderItemQuery) Modify(modifiers ...func(s *sql.Selector)) *PickupOrderItemSelect {
	poiq.modifiers = append(poiq.modifiers, modifiers...)
	return poiq.Select()
}

// PickupOrderItemGroupBy is the group-by builder for PickupOrderItem entities.
type PickupOrderItemGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (poigb *PickupOrderItemGroupBy) Aggregate(fns ...AggregateFunc) *PickupOrderItemGroupBy {
	poigb.fns = append(poigb.fns, fns...)
	return poigb
}

// Scan applies the group-by query and scans the result into the given value.
func (poigb *PickupOrderItemGroupBy) Scan(ctx context.Context, v any) error {
	query, err := poigb.path(ctx)
	if err != nil {
		return err
	}
	poigb.sql = query
	return poigb.sqlScan(ctx, v)
}

func (poigb *PickupOrderItemGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range poigb.fields {
		if !pickuporderitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := poigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := poigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (poigb *PickupOrderItemGroupBy) sqlQuery() *sql.Selector {
	selector := poigb.sql.Select()
	aggregation := make([]string, 0, len(poigb.fns))
	for _, fn := range poigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(poigb.fields)+len(poigb.fns))
		for _, f := range poigb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(poigb.fields...)...)
}

// PickupOrderItemSelect is the builder for selecting fields of PickupOrderItem entities.
type PickupOrderItemSelect struct {
	*PickupOrderItemQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pois *PickupOrderItemSelect) Aggregate(fns ...AggregateFunc) *PickupOrderItemSelect {
	pois.fns = append(pois.fns, fns...)
	return pois
}

// Scan applies the selector query and scans the result into the given value.
func (pois *PickupOrderItemSelect) Scan(ctx context.Context, v any) error {
	if err := pois.prepareQuery(ctx); err != nil {
		return err
	}
	pois.sql = pois.PickupOrderItemQuery.sqlQuery(ctx)
	return pois.sqlScan(ctx, v)
}

func (pois *PickupOrderItemSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(pois.fns))
	for _, fn := range pois.fns {
		aggregation = append(aggregation, fn(pois.sql))
	}
	switch n := len(*pois.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		pois.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		pois.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := pois.sql.Query()
	if err := pois.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pois *PickupOrderItemSelect) Modify(modifiers ...func(s *sql.Selector)) *PickupOrderItemSelect {
	pois.modifiers = append(pois.modifiers, modifiers...)
	return pois
}
