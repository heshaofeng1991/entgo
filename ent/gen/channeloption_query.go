// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/heshaofeng1991/entgo/ent/gen/channeloption"
	"github.com/heshaofeng1991/entgo/ent/gen/internal"
	"github.com/heshaofeng1991/entgo/ent/gen/predicate"
)

// ChannelOptionQuery is the builder for querying ChannelOption entities.
type ChannelOptionQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ChannelOption
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ChannelOptionQuery builder.
func (coq *ChannelOptionQuery) Where(ps ...predicate.ChannelOption) *ChannelOptionQuery {
	coq.predicates = append(coq.predicates, ps...)
	return coq
}

// Limit adds a limit step to the query.
func (coq *ChannelOptionQuery) Limit(limit int) *ChannelOptionQuery {
	coq.limit = &limit
	return coq
}

// Offset adds an offset step to the query.
func (coq *ChannelOptionQuery) Offset(offset int) *ChannelOptionQuery {
	coq.offset = &offset
	return coq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (coq *ChannelOptionQuery) Unique(unique bool) *ChannelOptionQuery {
	coq.unique = &unique
	return coq
}

// Order adds an order step to the query.
func (coq *ChannelOptionQuery) Order(o ...OrderFunc) *ChannelOptionQuery {
	coq.order = append(coq.order, o...)
	return coq
}

// First returns the first ChannelOption entity from the query.
// Returns a *NotFoundError when no ChannelOption was found.
func (coq *ChannelOptionQuery) First(ctx context.Context) (*ChannelOption, error) {
	nodes, err := coq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{channeloption.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (coq *ChannelOptionQuery) FirstX(ctx context.Context) *ChannelOption {
	node, err := coq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ChannelOption ID from the query.
// Returns a *NotFoundError when no ChannelOption ID was found.
func (coq *ChannelOptionQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = coq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{channeloption.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (coq *ChannelOptionQuery) FirstIDX(ctx context.Context) int64 {
	id, err := coq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ChannelOption entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ChannelOption entity is found.
// Returns a *NotFoundError when no ChannelOption entities are found.
func (coq *ChannelOptionQuery) Only(ctx context.Context) (*ChannelOption, error) {
	nodes, err := coq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{channeloption.Label}
	default:
		return nil, &NotSingularError{channeloption.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (coq *ChannelOptionQuery) OnlyX(ctx context.Context) *ChannelOption {
	node, err := coq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ChannelOption ID in the query.
// Returns a *NotSingularError when more than one ChannelOption ID is found.
// Returns a *NotFoundError when no entities are found.
func (coq *ChannelOptionQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = coq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{channeloption.Label}
	default:
		err = &NotSingularError{channeloption.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (coq *ChannelOptionQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := coq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ChannelOptions.
func (coq *ChannelOptionQuery) All(ctx context.Context) ([]*ChannelOption, error) {
	if err := coq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return coq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (coq *ChannelOptionQuery) AllX(ctx context.Context) []*ChannelOption {
	nodes, err := coq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ChannelOption IDs.
func (coq *ChannelOptionQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := coq.Select(channeloption.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (coq *ChannelOptionQuery) IDsX(ctx context.Context) []int64 {
	ids, err := coq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (coq *ChannelOptionQuery) Count(ctx context.Context) (int, error) {
	if err := coq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return coq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (coq *ChannelOptionQuery) CountX(ctx context.Context) int {
	count, err := coq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (coq *ChannelOptionQuery) Exist(ctx context.Context) (bool, error) {
	if err := coq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return coq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (coq *ChannelOptionQuery) ExistX(ctx context.Context) bool {
	exist, err := coq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ChannelOptionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (coq *ChannelOptionQuery) Clone() *ChannelOptionQuery {
	if coq == nil {
		return nil
	}
	return &ChannelOptionQuery{
		config:     coq.config,
		limit:      coq.limit,
		offset:     coq.offset,
		order:      append([]OrderFunc{}, coq.order...),
		predicates: append([]predicate.ChannelOption{}, coq.predicates...),
		// clone intermediate query.
		sql:    coq.sql.Clone(),
		path:   coq.path,
		unique: coq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OrderID int64 `json:"order_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ChannelOption.Query().
//		GroupBy(channeloption.FieldOrderID).
//		Aggregate(gen.Count()).
//		Scan(ctx, &v)
func (coq *ChannelOptionQuery) GroupBy(field string, fields ...string) *ChannelOptionGroupBy {
	grbuild := &ChannelOptionGroupBy{config: coq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := coq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return coq.sqlQuery(ctx), nil
	}
	grbuild.label = channeloption.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OrderID int64 `json:"order_id,omitempty"`
//	}
//
//	client.ChannelOption.Query().
//		Select(channeloption.FieldOrderID).
//		Scan(ctx, &v)
func (coq *ChannelOptionQuery) Select(fields ...string) *ChannelOptionSelect {
	coq.fields = append(coq.fields, fields...)
	selbuild := &ChannelOptionSelect{ChannelOptionQuery: coq}
	selbuild.label = channeloption.Label
	selbuild.flds, selbuild.scan = &coq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a ChannelOptionSelect configured with the given aggregations.
func (coq *ChannelOptionQuery) Aggregate(fns ...AggregateFunc) *ChannelOptionSelect {
	return coq.Select().Aggregate(fns...)
}

func (coq *ChannelOptionQuery) prepareQuery(ctx context.Context) error {
	for _, f := range coq.fields {
		if !channeloption.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("gen: invalid field %q for query", f)}
		}
	}
	if coq.path != nil {
		prev, err := coq.path(ctx)
		if err != nil {
			return err
		}
		coq.sql = prev
	}
	return nil
}

func (coq *ChannelOptionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ChannelOption, error) {
	var (
		nodes = []*ChannelOption{}
		_spec = coq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ChannelOption).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ChannelOption{config: coq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = coq.schemaConfig.ChannelOption
	ctx = internal.NewSchemaConfigContext(ctx, coq.schemaConfig)
	if len(coq.modifiers) > 0 {
		_spec.Modifiers = coq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, coq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (coq *ChannelOptionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := coq.querySpec()
	_spec.Node.Schema = coq.schemaConfig.ChannelOption
	ctx = internal.NewSchemaConfigContext(ctx, coq.schemaConfig)
	if len(coq.modifiers) > 0 {
		_spec.Modifiers = coq.modifiers
	}
	_spec.Node.Columns = coq.fields
	if len(coq.fields) > 0 {
		_spec.Unique = coq.unique != nil && *coq.unique
	}
	return sqlgraph.CountNodes(ctx, coq.driver, _spec)
}

func (coq *ChannelOptionQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := coq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("gen: check existence: %w", err)
	default:
		return true, nil
	}
}

func (coq *ChannelOptionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   channeloption.Table,
			Columns: channeloption.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: channeloption.FieldID,
			},
		},
		From:   coq.sql,
		Unique: true,
	}
	if unique := coq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := coq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, channeloption.FieldID)
		for i := range fields {
			if fields[i] != channeloption.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := coq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := coq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := coq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := coq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (coq *ChannelOptionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(coq.driver.Dialect())
	t1 := builder.Table(channeloption.Table)
	columns := coq.fields
	if len(columns) == 0 {
		columns = channeloption.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if coq.sql != nil {
		selector = coq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if coq.unique != nil && *coq.unique {
		selector.Distinct()
	}
	t1.Schema(coq.schemaConfig.ChannelOption)
	ctx = internal.NewSchemaConfigContext(ctx, coq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range coq.modifiers {
		m(selector)
	}
	for _, p := range coq.predicates {
		p(selector)
	}
	for _, p := range coq.order {
		p(selector)
	}
	if offset := coq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := coq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (coq *ChannelOptionQuery) Modify(modifiers ...func(s *sql.Selector)) *ChannelOptionSelect {
	coq.modifiers = append(coq.modifiers, modifiers...)
	return coq.Select()
}

// ChannelOptionGroupBy is the group-by builder for ChannelOption entities.
type ChannelOptionGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cogb *ChannelOptionGroupBy) Aggregate(fns ...AggregateFunc) *ChannelOptionGroupBy {
	cogb.fns = append(cogb.fns, fns...)
	return cogb
}

// Scan applies the group-by query and scans the result into the given value.
func (cogb *ChannelOptionGroupBy) Scan(ctx context.Context, v any) error {
	query, err := cogb.path(ctx)
	if err != nil {
		return err
	}
	cogb.sql = query
	return cogb.sqlScan(ctx, v)
}

func (cogb *ChannelOptionGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range cogb.fields {
		if !channeloption.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cogb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cogb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cogb *ChannelOptionGroupBy) sqlQuery() *sql.Selector {
	selector := cogb.sql.Select()
	aggregation := make([]string, 0, len(cogb.fns))
	for _, fn := range cogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cogb.fields)+len(cogb.fns))
		for _, f := range cogb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cogb.fields...)...)
}

// ChannelOptionSelect is the builder for selecting fields of ChannelOption entities.
type ChannelOptionSelect struct {
	*ChannelOptionQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cos *ChannelOptionSelect) Aggregate(fns ...AggregateFunc) *ChannelOptionSelect {
	cos.fns = append(cos.fns, fns...)
	return cos
}

// Scan applies the selector query and scans the result into the given value.
func (cos *ChannelOptionSelect) Scan(ctx context.Context, v any) error {
	if err := cos.prepareQuery(ctx); err != nil {
		return err
	}
	cos.sql = cos.ChannelOptionQuery.sqlQuery(ctx)
	return cos.sqlScan(ctx, v)
}

func (cos *ChannelOptionSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(cos.fns))
	for _, fn := range cos.fns {
		aggregation = append(aggregation, fn(cos.sql))
	}
	switch n := len(*cos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		cos.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		cos.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := cos.sql.Query()
	if err := cos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cos *ChannelOptionSelect) Modify(modifiers ...func(s *sql.Selector)) *ChannelOptionSelect {
	cos.modifiers = append(cos.modifiers, modifiers...)
	return cos
}
