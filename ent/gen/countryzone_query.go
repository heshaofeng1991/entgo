// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/heshaofeng1991/entgo/ent/gen/countryzone"
	"github.com/heshaofeng1991/entgo/ent/gen/internal"
	"github.com/heshaofeng1991/entgo/ent/gen/predicate"
)

// CountryZoneQuery is the builder for querying CountryZone entities.
type CountryZoneQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CountryZone
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CountryZoneQuery builder.
func (czq *CountryZoneQuery) Where(ps ...predicate.CountryZone) *CountryZoneQuery {
	czq.predicates = append(czq.predicates, ps...)
	return czq
}

// Limit adds a limit step to the query.
func (czq *CountryZoneQuery) Limit(limit int) *CountryZoneQuery {
	czq.limit = &limit
	return czq
}

// Offset adds an offset step to the query.
func (czq *CountryZoneQuery) Offset(offset int) *CountryZoneQuery {
	czq.offset = &offset
	return czq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (czq *CountryZoneQuery) Unique(unique bool) *CountryZoneQuery {
	czq.unique = &unique
	return czq
}

// Order adds an order step to the query.
func (czq *CountryZoneQuery) Order(o ...OrderFunc) *CountryZoneQuery {
	czq.order = append(czq.order, o...)
	return czq
}

// First returns the first CountryZone entity from the query.
// Returns a *NotFoundError when no CountryZone was found.
func (czq *CountryZoneQuery) First(ctx context.Context) (*CountryZone, error) {
	nodes, err := czq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{countryzone.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (czq *CountryZoneQuery) FirstX(ctx context.Context) *CountryZone {
	node, err := czq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CountryZone ID from the query.
// Returns a *NotFoundError when no CountryZone ID was found.
func (czq *CountryZoneQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = czq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{countryzone.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (czq *CountryZoneQuery) FirstIDX(ctx context.Context) int {
	id, err := czq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CountryZone entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CountryZone entity is found.
// Returns a *NotFoundError when no CountryZone entities are found.
func (czq *CountryZoneQuery) Only(ctx context.Context) (*CountryZone, error) {
	nodes, err := czq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{countryzone.Label}
	default:
		return nil, &NotSingularError{countryzone.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (czq *CountryZoneQuery) OnlyX(ctx context.Context) *CountryZone {
	node, err := czq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CountryZone ID in the query.
// Returns a *NotSingularError when more than one CountryZone ID is found.
// Returns a *NotFoundError when no entities are found.
func (czq *CountryZoneQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = czq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{countryzone.Label}
	default:
		err = &NotSingularError{countryzone.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (czq *CountryZoneQuery) OnlyIDX(ctx context.Context) int {
	id, err := czq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CountryZones.
func (czq *CountryZoneQuery) All(ctx context.Context) ([]*CountryZone, error) {
	if err := czq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return czq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (czq *CountryZoneQuery) AllX(ctx context.Context) []*CountryZone {
	nodes, err := czq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CountryZone IDs.
func (czq *CountryZoneQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := czq.Select(countryzone.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (czq *CountryZoneQuery) IDsX(ctx context.Context) []int {
	ids, err := czq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (czq *CountryZoneQuery) Count(ctx context.Context) (int, error) {
	if err := czq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return czq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (czq *CountryZoneQuery) CountX(ctx context.Context) int {
	count, err := czq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (czq *CountryZoneQuery) Exist(ctx context.Context) (bool, error) {
	if err := czq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return czq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (czq *CountryZoneQuery) ExistX(ctx context.Context) bool {
	exist, err := czq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CountryZoneQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (czq *CountryZoneQuery) Clone() *CountryZoneQuery {
	if czq == nil {
		return nil
	}
	return &CountryZoneQuery{
		config:     czq.config,
		limit:      czq.limit,
		offset:     czq.offset,
		order:      append([]OrderFunc{}, czq.order...),
		predicates: append([]predicate.CountryZone{}, czq.predicates...),
		// clone intermediate query.
		sql:    czq.sql.Clone(),
		path:   czq.path,
		unique: czq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ChannelID int64 `json:"channel_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CountryZone.Query().
//		GroupBy(countryzone.FieldChannelID).
//		Aggregate(gen.Count()).
//		Scan(ctx, &v)
func (czq *CountryZoneQuery) GroupBy(field string, fields ...string) *CountryZoneGroupBy {
	grbuild := &CountryZoneGroupBy{config: czq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := czq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return czq.sqlQuery(ctx), nil
	}
	grbuild.label = countryzone.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ChannelID int64 `json:"channel_id,omitempty"`
//	}
//
//	client.CountryZone.Query().
//		Select(countryzone.FieldChannelID).
//		Scan(ctx, &v)
func (czq *CountryZoneQuery) Select(fields ...string) *CountryZoneSelect {
	czq.fields = append(czq.fields, fields...)
	selbuild := &CountryZoneSelect{CountryZoneQuery: czq}
	selbuild.label = countryzone.Label
	selbuild.flds, selbuild.scan = &czq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a CountryZoneSelect configured with the given aggregations.
func (czq *CountryZoneQuery) Aggregate(fns ...AggregateFunc) *CountryZoneSelect {
	return czq.Select().Aggregate(fns...)
}

func (czq *CountryZoneQuery) prepareQuery(ctx context.Context) error {
	for _, f := range czq.fields {
		if !countryzone.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("gen: invalid field %q for query", f)}
		}
	}
	if czq.path != nil {
		prev, err := czq.path(ctx)
		if err != nil {
			return err
		}
		czq.sql = prev
	}
	return nil
}

func (czq *CountryZoneQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CountryZone, error) {
	var (
		nodes = []*CountryZone{}
		_spec = czq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CountryZone).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CountryZone{config: czq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = czq.schemaConfig.CountryZone
	ctx = internal.NewSchemaConfigContext(ctx, czq.schemaConfig)
	if len(czq.modifiers) > 0 {
		_spec.Modifiers = czq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, czq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (czq *CountryZoneQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := czq.querySpec()
	_spec.Node.Schema = czq.schemaConfig.CountryZone
	ctx = internal.NewSchemaConfigContext(ctx, czq.schemaConfig)
	if len(czq.modifiers) > 0 {
		_spec.Modifiers = czq.modifiers
	}
	_spec.Node.Columns = czq.fields
	if len(czq.fields) > 0 {
		_spec.Unique = czq.unique != nil && *czq.unique
	}
	return sqlgraph.CountNodes(ctx, czq.driver, _spec)
}

func (czq *CountryZoneQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := czq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("gen: check existence: %w", err)
	default:
		return true, nil
	}
}

func (czq *CountryZoneQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   countryzone.Table,
			Columns: countryzone.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: countryzone.FieldID,
			},
		},
		From:   czq.sql,
		Unique: true,
	}
	if unique := czq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := czq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, countryzone.FieldID)
		for i := range fields {
			if fields[i] != countryzone.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := czq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := czq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := czq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := czq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (czq *CountryZoneQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(czq.driver.Dialect())
	t1 := builder.Table(countryzone.Table)
	columns := czq.fields
	if len(columns) == 0 {
		columns = countryzone.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if czq.sql != nil {
		selector = czq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if czq.unique != nil && *czq.unique {
		selector.Distinct()
	}
	t1.Schema(czq.schemaConfig.CountryZone)
	ctx = internal.NewSchemaConfigContext(ctx, czq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range czq.modifiers {
		m(selector)
	}
	for _, p := range czq.predicates {
		p(selector)
	}
	for _, p := range czq.order {
		p(selector)
	}
	if offset := czq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := czq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (czq *CountryZoneQuery) Modify(modifiers ...func(s *sql.Selector)) *CountryZoneSelect {
	czq.modifiers = append(czq.modifiers, modifiers...)
	return czq.Select()
}

// CountryZoneGroupBy is the group-by builder for CountryZone entities.
type CountryZoneGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (czgb *CountryZoneGroupBy) Aggregate(fns ...AggregateFunc) *CountryZoneGroupBy {
	czgb.fns = append(czgb.fns, fns...)
	return czgb
}

// Scan applies the group-by query and scans the result into the given value.
func (czgb *CountryZoneGroupBy) Scan(ctx context.Context, v any) error {
	query, err := czgb.path(ctx)
	if err != nil {
		return err
	}
	czgb.sql = query
	return czgb.sqlScan(ctx, v)
}

func (czgb *CountryZoneGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range czgb.fields {
		if !countryzone.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := czgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := czgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (czgb *CountryZoneGroupBy) sqlQuery() *sql.Selector {
	selector := czgb.sql.Select()
	aggregation := make([]string, 0, len(czgb.fns))
	for _, fn := range czgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(czgb.fields)+len(czgb.fns))
		for _, f := range czgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(czgb.fields...)...)
}

// CountryZoneSelect is the builder for selecting fields of CountryZone entities.
type CountryZoneSelect struct {
	*CountryZoneQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (czs *CountryZoneSelect) Aggregate(fns ...AggregateFunc) *CountryZoneSelect {
	czs.fns = append(czs.fns, fns...)
	return czs
}

// Scan applies the selector query and scans the result into the given value.
func (czs *CountryZoneSelect) Scan(ctx context.Context, v any) error {
	if err := czs.prepareQuery(ctx); err != nil {
		return err
	}
	czs.sql = czs.CountryZoneQuery.sqlQuery(ctx)
	return czs.sqlScan(ctx, v)
}

func (czs *CountryZoneSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(czs.fns))
	for _, fn := range czs.fns {
		aggregation = append(aggregation, fn(czs.sql))
	}
	switch n := len(*czs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		czs.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		czs.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := czs.sql.Query()
	if err := czs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (czs *CountryZoneSelect) Modify(modifiers ...func(s *sql.Selector)) *CountryZoneSelect {
	czs.modifiers = append(czs.modifiers, modifiers...)
	return czs
}
