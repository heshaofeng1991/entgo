// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/heshaofeng1991/entgo/ent/gen/channelvolumefactor"
	"github.com/heshaofeng1991/entgo/ent/gen/internal"
	"github.com/heshaofeng1991/entgo/ent/gen/predicate"
)

// ChannelVolumeFactorQuery is the builder for querying ChannelVolumeFactor entities.
type ChannelVolumeFactorQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ChannelVolumeFactor
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ChannelVolumeFactorQuery builder.
func (cvfq *ChannelVolumeFactorQuery) Where(ps ...predicate.ChannelVolumeFactor) *ChannelVolumeFactorQuery {
	cvfq.predicates = append(cvfq.predicates, ps...)
	return cvfq
}

// Limit adds a limit step to the query.
func (cvfq *ChannelVolumeFactorQuery) Limit(limit int) *ChannelVolumeFactorQuery {
	cvfq.limit = &limit
	return cvfq
}

// Offset adds an offset step to the query.
func (cvfq *ChannelVolumeFactorQuery) Offset(offset int) *ChannelVolumeFactorQuery {
	cvfq.offset = &offset
	return cvfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cvfq *ChannelVolumeFactorQuery) Unique(unique bool) *ChannelVolumeFactorQuery {
	cvfq.unique = &unique
	return cvfq
}

// Order adds an order step to the query.
func (cvfq *ChannelVolumeFactorQuery) Order(o ...OrderFunc) *ChannelVolumeFactorQuery {
	cvfq.order = append(cvfq.order, o...)
	return cvfq
}

// First returns the first ChannelVolumeFactor entity from the query.
// Returns a *NotFoundError when no ChannelVolumeFactor was found.
func (cvfq *ChannelVolumeFactorQuery) First(ctx context.Context) (*ChannelVolumeFactor, error) {
	nodes, err := cvfq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{channelvolumefactor.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cvfq *ChannelVolumeFactorQuery) FirstX(ctx context.Context) *ChannelVolumeFactor {
	node, err := cvfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ChannelVolumeFactor ID from the query.
// Returns a *NotFoundError when no ChannelVolumeFactor ID was found.
func (cvfq *ChannelVolumeFactorQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cvfq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{channelvolumefactor.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cvfq *ChannelVolumeFactorQuery) FirstIDX(ctx context.Context) int64 {
	id, err := cvfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ChannelVolumeFactor entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ChannelVolumeFactor entity is found.
// Returns a *NotFoundError when no ChannelVolumeFactor entities are found.
func (cvfq *ChannelVolumeFactorQuery) Only(ctx context.Context) (*ChannelVolumeFactor, error) {
	nodes, err := cvfq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{channelvolumefactor.Label}
	default:
		return nil, &NotSingularError{channelvolumefactor.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cvfq *ChannelVolumeFactorQuery) OnlyX(ctx context.Context) *ChannelVolumeFactor {
	node, err := cvfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ChannelVolumeFactor ID in the query.
// Returns a *NotSingularError when more than one ChannelVolumeFactor ID is found.
// Returns a *NotFoundError when no entities are found.
func (cvfq *ChannelVolumeFactorQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cvfq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{channelvolumefactor.Label}
	default:
		err = &NotSingularError{channelvolumefactor.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cvfq *ChannelVolumeFactorQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := cvfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ChannelVolumeFactors.
func (cvfq *ChannelVolumeFactorQuery) All(ctx context.Context) ([]*ChannelVolumeFactor, error) {
	if err := cvfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cvfq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cvfq *ChannelVolumeFactorQuery) AllX(ctx context.Context) []*ChannelVolumeFactor {
	nodes, err := cvfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ChannelVolumeFactor IDs.
func (cvfq *ChannelVolumeFactorQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := cvfq.Select(channelvolumefactor.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cvfq *ChannelVolumeFactorQuery) IDsX(ctx context.Context) []int64 {
	ids, err := cvfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cvfq *ChannelVolumeFactorQuery) Count(ctx context.Context) (int, error) {
	if err := cvfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cvfq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cvfq *ChannelVolumeFactorQuery) CountX(ctx context.Context) int {
	count, err := cvfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cvfq *ChannelVolumeFactorQuery) Exist(ctx context.Context) (bool, error) {
	if err := cvfq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cvfq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cvfq *ChannelVolumeFactorQuery) ExistX(ctx context.Context) bool {
	exist, err := cvfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ChannelVolumeFactorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cvfq *ChannelVolumeFactorQuery) Clone() *ChannelVolumeFactorQuery {
	if cvfq == nil {
		return nil
	}
	return &ChannelVolumeFactorQuery{
		config:     cvfq.config,
		limit:      cvfq.limit,
		offset:     cvfq.offset,
		order:      append([]OrderFunc{}, cvfq.order...),
		predicates: append([]predicate.ChannelVolumeFactor{}, cvfq.predicates...),
		// clone intermediate query.
		sql:    cvfq.sql.Clone(),
		path:   cvfq.path,
		unique: cvfq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ChannelVolumeFactor.Query().
//		GroupBy(channelvolumefactor.FieldCreatedAt).
//		Aggregate(gen.Count()).
//		Scan(ctx, &v)
func (cvfq *ChannelVolumeFactorQuery) GroupBy(field string, fields ...string) *ChannelVolumeFactorGroupBy {
	grbuild := &ChannelVolumeFactorGroupBy{config: cvfq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cvfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cvfq.sqlQuery(ctx), nil
	}
	grbuild.label = channelvolumefactor.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ChannelVolumeFactor.Query().
//		Select(channelvolumefactor.FieldCreatedAt).
//		Scan(ctx, &v)
func (cvfq *ChannelVolumeFactorQuery) Select(fields ...string) *ChannelVolumeFactorSelect {
	cvfq.fields = append(cvfq.fields, fields...)
	selbuild := &ChannelVolumeFactorSelect{ChannelVolumeFactorQuery: cvfq}
	selbuild.label = channelvolumefactor.Label
	selbuild.flds, selbuild.scan = &cvfq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a ChannelVolumeFactorSelect configured with the given aggregations.
func (cvfq *ChannelVolumeFactorQuery) Aggregate(fns ...AggregateFunc) *ChannelVolumeFactorSelect {
	return cvfq.Select().Aggregate(fns...)
}

func (cvfq *ChannelVolumeFactorQuery) prepareQuery(ctx context.Context) error {
	for _, f := range cvfq.fields {
		if !channelvolumefactor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("gen: invalid field %q for query", f)}
		}
	}
	if cvfq.path != nil {
		prev, err := cvfq.path(ctx)
		if err != nil {
			return err
		}
		cvfq.sql = prev
	}
	return nil
}

func (cvfq *ChannelVolumeFactorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ChannelVolumeFactor, error) {
	var (
		nodes = []*ChannelVolumeFactor{}
		_spec = cvfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ChannelVolumeFactor).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ChannelVolumeFactor{config: cvfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = cvfq.schemaConfig.ChannelVolumeFactor
	ctx = internal.NewSchemaConfigContext(ctx, cvfq.schemaConfig)
	if len(cvfq.modifiers) > 0 {
		_spec.Modifiers = cvfq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cvfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cvfq *ChannelVolumeFactorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cvfq.querySpec()
	_spec.Node.Schema = cvfq.schemaConfig.ChannelVolumeFactor
	ctx = internal.NewSchemaConfigContext(ctx, cvfq.schemaConfig)
	if len(cvfq.modifiers) > 0 {
		_spec.Modifiers = cvfq.modifiers
	}
	_spec.Node.Columns = cvfq.fields
	if len(cvfq.fields) > 0 {
		_spec.Unique = cvfq.unique != nil && *cvfq.unique
	}
	return sqlgraph.CountNodes(ctx, cvfq.driver, _spec)
}

func (cvfq *ChannelVolumeFactorQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := cvfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("gen: check existence: %w", err)
	default:
		return true, nil
	}
}

func (cvfq *ChannelVolumeFactorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   channelvolumefactor.Table,
			Columns: channelvolumefactor.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: channelvolumefactor.FieldID,
			},
		},
		From:   cvfq.sql,
		Unique: true,
	}
	if unique := cvfq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := cvfq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, channelvolumefactor.FieldID)
		for i := range fields {
			if fields[i] != channelvolumefactor.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cvfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cvfq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cvfq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cvfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cvfq *ChannelVolumeFactorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cvfq.driver.Dialect())
	t1 := builder.Table(channelvolumefactor.Table)
	columns := cvfq.fields
	if len(columns) == 0 {
		columns = channelvolumefactor.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cvfq.sql != nil {
		selector = cvfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cvfq.unique != nil && *cvfq.unique {
		selector.Distinct()
	}
	t1.Schema(cvfq.schemaConfig.ChannelVolumeFactor)
	ctx = internal.NewSchemaConfigContext(ctx, cvfq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range cvfq.modifiers {
		m(selector)
	}
	for _, p := range cvfq.predicates {
		p(selector)
	}
	for _, p := range cvfq.order {
		p(selector)
	}
	if offset := cvfq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cvfq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cvfq *ChannelVolumeFactorQuery) Modify(modifiers ...func(s *sql.Selector)) *ChannelVolumeFactorSelect {
	cvfq.modifiers = append(cvfq.modifiers, modifiers...)
	return cvfq.Select()
}

// ChannelVolumeFactorGroupBy is the group-by builder for ChannelVolumeFactor entities.
type ChannelVolumeFactorGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cvfgb *ChannelVolumeFactorGroupBy) Aggregate(fns ...AggregateFunc) *ChannelVolumeFactorGroupBy {
	cvfgb.fns = append(cvfgb.fns, fns...)
	return cvfgb
}

// Scan applies the group-by query and scans the result into the given value.
func (cvfgb *ChannelVolumeFactorGroupBy) Scan(ctx context.Context, v any) error {
	query, err := cvfgb.path(ctx)
	if err != nil {
		return err
	}
	cvfgb.sql = query
	return cvfgb.sqlScan(ctx, v)
}

func (cvfgb *ChannelVolumeFactorGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range cvfgb.fields {
		if !channelvolumefactor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cvfgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cvfgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cvfgb *ChannelVolumeFactorGroupBy) sqlQuery() *sql.Selector {
	selector := cvfgb.sql.Select()
	aggregation := make([]string, 0, len(cvfgb.fns))
	for _, fn := range cvfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cvfgb.fields)+len(cvfgb.fns))
		for _, f := range cvfgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cvfgb.fields...)...)
}

// ChannelVolumeFactorSelect is the builder for selecting fields of ChannelVolumeFactor entities.
type ChannelVolumeFactorSelect struct {
	*ChannelVolumeFactorQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cvfs *ChannelVolumeFactorSelect) Aggregate(fns ...AggregateFunc) *ChannelVolumeFactorSelect {
	cvfs.fns = append(cvfs.fns, fns...)
	return cvfs
}

// Scan applies the selector query and scans the result into the given value.
func (cvfs *ChannelVolumeFactorSelect) Scan(ctx context.Context, v any) error {
	if err := cvfs.prepareQuery(ctx); err != nil {
		return err
	}
	cvfs.sql = cvfs.ChannelVolumeFactorQuery.sqlQuery(ctx)
	return cvfs.sqlScan(ctx, v)
}

func (cvfs *ChannelVolumeFactorSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(cvfs.fns))
	for _, fn := range cvfs.fns {
		aggregation = append(aggregation, fn(cvfs.sql))
	}
	switch n := len(*cvfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		cvfs.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		cvfs.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := cvfs.sql.Query()
	if err := cvfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cvfs *ChannelVolumeFactorSelect) Modify(modifiers ...func(s *sql.Selector)) *ChannelVolumeFactorSelect {
	cvfs.modifiers = append(cvfs.modifiers, modifiers...)
	return cvfs
}
