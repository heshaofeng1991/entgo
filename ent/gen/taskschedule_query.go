// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/heshaofeng1991/entgo/ent/gen/internal"
	"github.com/heshaofeng1991/entgo/ent/gen/predicate"
	"github.com/heshaofeng1991/entgo/ent/gen/taskschedule"
)

// TaskScheduleQuery is the builder for querying TaskSchedule entities.
type TaskScheduleQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.TaskSchedule
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TaskScheduleQuery builder.
func (tsq *TaskScheduleQuery) Where(ps ...predicate.TaskSchedule) *TaskScheduleQuery {
	tsq.predicates = append(tsq.predicates, ps...)
	return tsq
}

// Limit adds a limit step to the query.
func (tsq *TaskScheduleQuery) Limit(limit int) *TaskScheduleQuery {
	tsq.limit = &limit
	return tsq
}

// Offset adds an offset step to the query.
func (tsq *TaskScheduleQuery) Offset(offset int) *TaskScheduleQuery {
	tsq.offset = &offset
	return tsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tsq *TaskScheduleQuery) Unique(unique bool) *TaskScheduleQuery {
	tsq.unique = &unique
	return tsq
}

// Order adds an order step to the query.
func (tsq *TaskScheduleQuery) Order(o ...OrderFunc) *TaskScheduleQuery {
	tsq.order = append(tsq.order, o...)
	return tsq
}

// First returns the first TaskSchedule entity from the query.
// Returns a *NotFoundError when no TaskSchedule was found.
func (tsq *TaskScheduleQuery) First(ctx context.Context) (*TaskSchedule, error) {
	nodes, err := tsq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{taskschedule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tsq *TaskScheduleQuery) FirstX(ctx context.Context) *TaskSchedule {
	node, err := tsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TaskSchedule ID from the query.
// Returns a *NotFoundError when no TaskSchedule ID was found.
func (tsq *TaskScheduleQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = tsq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{taskschedule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tsq *TaskScheduleQuery) FirstIDX(ctx context.Context) int64 {
	id, err := tsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TaskSchedule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TaskSchedule entity is found.
// Returns a *NotFoundError when no TaskSchedule entities are found.
func (tsq *TaskScheduleQuery) Only(ctx context.Context) (*TaskSchedule, error) {
	nodes, err := tsq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{taskschedule.Label}
	default:
		return nil, &NotSingularError{taskschedule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tsq *TaskScheduleQuery) OnlyX(ctx context.Context) *TaskSchedule {
	node, err := tsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TaskSchedule ID in the query.
// Returns a *NotSingularError when more than one TaskSchedule ID is found.
// Returns a *NotFoundError when no entities are found.
func (tsq *TaskScheduleQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = tsq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{taskschedule.Label}
	default:
		err = &NotSingularError{taskschedule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tsq *TaskScheduleQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := tsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TaskSchedules.
func (tsq *TaskScheduleQuery) All(ctx context.Context) ([]*TaskSchedule, error) {
	if err := tsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tsq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tsq *TaskScheduleQuery) AllX(ctx context.Context) []*TaskSchedule {
	nodes, err := tsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TaskSchedule IDs.
func (tsq *TaskScheduleQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := tsq.Select(taskschedule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tsq *TaskScheduleQuery) IDsX(ctx context.Context) []int64 {
	ids, err := tsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tsq *TaskScheduleQuery) Count(ctx context.Context) (int, error) {
	if err := tsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tsq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tsq *TaskScheduleQuery) CountX(ctx context.Context) int {
	count, err := tsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tsq *TaskScheduleQuery) Exist(ctx context.Context) (bool, error) {
	if err := tsq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tsq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tsq *TaskScheduleQuery) ExistX(ctx context.Context) bool {
	exist, err := tsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TaskScheduleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tsq *TaskScheduleQuery) Clone() *TaskScheduleQuery {
	if tsq == nil {
		return nil
	}
	return &TaskScheduleQuery{
		config:     tsq.config,
		limit:      tsq.limit,
		offset:     tsq.offset,
		order:      append([]OrderFunc{}, tsq.order...),
		predicates: append([]predicate.TaskSchedule{}, tsq.predicates...),
		// clone intermediate query.
		sql:    tsq.sql.Clone(),
		path:   tsq.path,
		unique: tsq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TaskSchedule.Query().
//		GroupBy(taskschedule.FieldCreatedAt).
//		Aggregate(gen.Count()).
//		Scan(ctx, &v)
func (tsq *TaskScheduleQuery) GroupBy(field string, fields ...string) *TaskScheduleGroupBy {
	grbuild := &TaskScheduleGroupBy{config: tsq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tsq.sqlQuery(ctx), nil
	}
	grbuild.label = taskschedule.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.TaskSchedule.Query().
//		Select(taskschedule.FieldCreatedAt).
//		Scan(ctx, &v)
func (tsq *TaskScheduleQuery) Select(fields ...string) *TaskScheduleSelect {
	tsq.fields = append(tsq.fields, fields...)
	selbuild := &TaskScheduleSelect{TaskScheduleQuery: tsq}
	selbuild.label = taskschedule.Label
	selbuild.flds, selbuild.scan = &tsq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a TaskScheduleSelect configured with the given aggregations.
func (tsq *TaskScheduleQuery) Aggregate(fns ...AggregateFunc) *TaskScheduleSelect {
	return tsq.Select().Aggregate(fns...)
}

func (tsq *TaskScheduleQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tsq.fields {
		if !taskschedule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("gen: invalid field %q for query", f)}
		}
	}
	if tsq.path != nil {
		prev, err := tsq.path(ctx)
		if err != nil {
			return err
		}
		tsq.sql = prev
	}
	return nil
}

func (tsq *TaskScheduleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TaskSchedule, error) {
	var (
		nodes = []*TaskSchedule{}
		_spec = tsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TaskSchedule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TaskSchedule{config: tsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = tsq.schemaConfig.TaskSchedule
	ctx = internal.NewSchemaConfigContext(ctx, tsq.schemaConfig)
	if len(tsq.modifiers) > 0 {
		_spec.Modifiers = tsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tsq *TaskScheduleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tsq.querySpec()
	_spec.Node.Schema = tsq.schemaConfig.TaskSchedule
	ctx = internal.NewSchemaConfigContext(ctx, tsq.schemaConfig)
	if len(tsq.modifiers) > 0 {
		_spec.Modifiers = tsq.modifiers
	}
	_spec.Node.Columns = tsq.fields
	if len(tsq.fields) > 0 {
		_spec.Unique = tsq.unique != nil && *tsq.unique
	}
	return sqlgraph.CountNodes(ctx, tsq.driver, _spec)
}

func (tsq *TaskScheduleQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := tsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("gen: check existence: %w", err)
	default:
		return true, nil
	}
}

func (tsq *TaskScheduleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   taskschedule.Table,
			Columns: taskschedule.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: taskschedule.FieldID,
			},
		},
		From:   tsq.sql,
		Unique: true,
	}
	if unique := tsq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tsq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, taskschedule.FieldID)
		for i := range fields {
			if fields[i] != taskschedule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tsq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tsq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tsq *TaskScheduleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tsq.driver.Dialect())
	t1 := builder.Table(taskschedule.Table)
	columns := tsq.fields
	if len(columns) == 0 {
		columns = taskschedule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tsq.sql != nil {
		selector = tsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tsq.unique != nil && *tsq.unique {
		selector.Distinct()
	}
	t1.Schema(tsq.schemaConfig.TaskSchedule)
	ctx = internal.NewSchemaConfigContext(ctx, tsq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range tsq.modifiers {
		m(selector)
	}
	for _, p := range tsq.predicates {
		p(selector)
	}
	for _, p := range tsq.order {
		p(selector)
	}
	if offset := tsq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tsq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tsq *TaskScheduleQuery) Modify(modifiers ...func(s *sql.Selector)) *TaskScheduleSelect {
	tsq.modifiers = append(tsq.modifiers, modifiers...)
	return tsq.Select()
}

// TaskScheduleGroupBy is the group-by builder for TaskSchedule entities.
type TaskScheduleGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tsgb *TaskScheduleGroupBy) Aggregate(fns ...AggregateFunc) *TaskScheduleGroupBy {
	tsgb.fns = append(tsgb.fns, fns...)
	return tsgb
}

// Scan applies the group-by query and scans the result into the given value.
func (tsgb *TaskScheduleGroupBy) Scan(ctx context.Context, v any) error {
	query, err := tsgb.path(ctx)
	if err != nil {
		return err
	}
	tsgb.sql = query
	return tsgb.sqlScan(ctx, v)
}

func (tsgb *TaskScheduleGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range tsgb.fields {
		if !taskschedule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tsgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tsgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tsgb *TaskScheduleGroupBy) sqlQuery() *sql.Selector {
	selector := tsgb.sql.Select()
	aggregation := make([]string, 0, len(tsgb.fns))
	for _, fn := range tsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tsgb.fields)+len(tsgb.fns))
		for _, f := range tsgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tsgb.fields...)...)
}

// TaskScheduleSelect is the builder for selecting fields of TaskSchedule entities.
type TaskScheduleSelect struct {
	*TaskScheduleQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tss *TaskScheduleSelect) Aggregate(fns ...AggregateFunc) *TaskScheduleSelect {
	tss.fns = append(tss.fns, fns...)
	return tss
}

// Scan applies the selector query and scans the result into the given value.
func (tss *TaskScheduleSelect) Scan(ctx context.Context, v any) error {
	if err := tss.prepareQuery(ctx); err != nil {
		return err
	}
	tss.sql = tss.TaskScheduleQuery.sqlQuery(ctx)
	return tss.sqlScan(ctx, v)
}

func (tss *TaskScheduleSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(tss.fns))
	for _, fn := range tss.fns {
		aggregation = append(aggregation, fn(tss.sql))
	}
	switch n := len(*tss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		tss.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		tss.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := tss.sql.Query()
	if err := tss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tss *TaskScheduleSelect) Modify(modifiers ...func(s *sql.Selector)) *TaskScheduleSelect {
	tss.modifiers = append(tss.modifiers, modifiers...)
	return tss
}
